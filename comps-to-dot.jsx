// export ae comp flow to .dot file for usage with graphvizArray.prototype.unique = function (){      var r = new Array();      o:for(var i = 0, n = this.length; i < n; i++){          for(var x = 0, y = r.length; x < y; x++){              if(r[x]==this[i]) continue o;}          r[r.length] = this[i];}      return r;  }  var arr = [];function getComps(comp){//~     $.writeln(comp.name);            if(comp.layers !== null){                for(var i = 1;i <= comp.numLayers;i++){                var l = comp.layer(i);                if(l.source.layers !== null){                arr.push("\"" + comp.name + "\" -> \"" + l.name + "\";");                                    getComps(l.source);                }                }                            }else{                        return;                    }                   };var main = function (){// app.beginUndoGroup("export");var curComp = app.project.activeItem;   if (!curComp || !(curComp instanceof CompItem)){        alert("noComp");        return;    };    getComps (curComp);//~ for(var i = 1; i <= curComp.numLayers;i++){//~ var l = curComp.layers[i];//~ if(l.layers !== null){//~   arr.push("\"" + curComp.name + "\" -> \"" + l.name + "\"");//~   }else{//~     $.writeln(l.name +" is "+  l.constructor.name );//~   }//~ }var u_arr = arr.unique ();$.writeln( "digraph {\n" + u_arr.join("\n") + "\n}");// app.endUndoGroup();};main();